<?php

// use Drupal\Core\Lock\LockBackendInterface;

// /**
//  * Check if Drupal cron is currently running.
//  */
// function isCronRunning() {
//   // Get the lock service.
//   $lock = \Drupal::service('lock');

//   // Check if the 'cron' lock is acquired.
//   if ($lock->isLocked('cron')) {
//     return "Cron is currently running.";
//   } else {
//     return "Cron is not running.";
//   }
// }

// // Example usage.
// $result = isCronRunning();
// \Drupal::logger('custom')->info($result);
